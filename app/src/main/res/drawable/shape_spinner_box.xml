<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- 배경색 설정 -->
    <corners android:radius="8dp" /> <!-- 모서리 둥글기 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/black" /> <!-- 테두리 색상 설정 -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="8dp"
        android:bottom="8dp"/>
</shape>
